#include <stdio.h>

int main()
{
    int arr[] = { 73, 32, 99, 97, 110, 32, 100, 111, 32, 105, 116, 33 };
    int sz = sizeof(arr) / sizeof(arr[0]);  // 计算数组的大小

    for (int i = 0; i < sz; i++)
    {
        printf("%c", arr[i]);  // 将数组中的整数值转换为字符并打印
    }

    return 0;  // 程序成功执行，返回 0
}
